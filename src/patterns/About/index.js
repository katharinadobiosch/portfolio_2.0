import SectionFrame from "../SectionFrame";

function About() {
    return (
        <div className="">
            <SectionFrame text="ABOUT" section="about" />
        </div>
    );
}

export default About;
