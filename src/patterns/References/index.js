import SectionFrame from "../SectionFrame";

function References() {
    return (
        <div className="">
            <SectionFrame text="REFERENCES" section="references"></SectionFrame>
        </div>
    );
}

export default References;
