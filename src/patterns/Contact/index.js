import SectionFrame from "../SectionFrame";

function Contact() {
    return (
        <div className="">
            <SectionFrame text="CONTACT"
            section="contact" />
        </div>
    );
}

export default Contact;
